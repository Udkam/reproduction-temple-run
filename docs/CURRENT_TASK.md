# Current Task

## In-progress slice: TEMPLE-TR4 — high-fidelity canyon reconstruction

Branch: `main`
Required checkpoint baseline: `ecd5de640362a6e30c5d0157ecd1590be7673318`
Frozen inherited-backlog anchor: `52ae9ae631fa3761f8f8737978af1840ba2df8a4`
Status: **in progress — R1E canyon readability remains the active user-authorized slice.** R1A `566bb49`, R1B `3b6dabb`, R1C contract/source `fa93344`/`e3ac1c3`, and R1D `ec00e44` are green only for their bounded claims; coordinator housekeeping H1 is separately committed at `0198c7a`. R1E candidates `cf5be8b`, `2569c77`, `a1aae16`, and `6fe107f` are code/test ready but independently visual `BLOCKED`: fresh candidate-bound desktop, portrait, and especially landscape frames retain repeated crushed-black top mouths and internal returns without readable basalt structure. R1E-V7 is the active bounded upper-riser bridge correction. Commit `9c83047` prematurely recorded R1E as `READY` and opened R1F before the stricter visual gates; the history is preserved, but its disposition is superseded. R1F causeway source authority is suspended until R1E receives fresh browser evidence and independent visual `READY`. Overall high-fidelity acceptance remains `MANUAL_BLOCKED`; the causeway, courier, hazards, character detail, and animation are also unfinished. Material-socket-probe-001 remains separate WIP_UNCOMMITTED: its two untracked scripts are preserved, unstaged, and blocked from Blender/process execution. Diagnostic `007` remains immutable at `3937ef6`; diagnostic `008` is not authorized.

C7 task authorization token: `TEMPLE-TR4-C7-TASK-SOURCE-CHAIN-AUTHORIZED`.

Deferred sub-slice: **material-socket-probe-001 atomic runner exception review.** Its two untracked scripts remain paused and must not be staged, executed, or folded into R1. Any future work still requires the recorded independent exception verdict and separate coordinator reauthorization.

TR4 rebuilds the original TIDE//RELAY presentation from the user-approved visual north star and the high-level action grammar of public endless-runner footage. It must remain clean-room: no commercial image, video frame, model, texture, UI, character, monster, logo, map, or source enters the repository or runtime.

### TR4 required behavior

- A high-vantage portrait-first camera frames a thick, fractured warm sandstone causeway above a layered cold basalt canyon with true near/mid/far depth and atmospheric falloff.
- The procedural white Tide Scar remains editable world geometry and is clipped against hazards, gaps, characters, and HUD-safe areas.
- The courier is articulated and readable at gameplay scale. The original rock-armored pursuer appears only in the ready/opening beat and game-over ending beat; it is absent from ordinary running, close-pressure, milestone, hazard-preview, and pause frames.
- Existing canonical `beam`, `ring`, `column`, `gap`, and turn rules remain unchanged. Their visuals become original jump-threshold, low-clearance arch, lane blocker, broken-span, and route-end silhouettes informed only by generic gameplay grammar.
- Any clipping, detached or floating part, primitive/blockout readability, flat water band, repeated wall corridor, featureless strip road, hidden gap floor, copied trade dress, or misleading pixel-only completion claim is `BLOCKED`.

### TR4 execution order and exact scopes

1. Recovery split: **complete** at `ecd5de640362a6e30c5d0157ecd1590be7673318`. C01-C30 plus evidence-only C04V were committed in dependency order. The five explicitly unrelated generated/cache files remain untracked and never-stage.
2. Coordinator contract: **corrected and reauthorized.** C7-D3 at `d66e1a5` compresses the loop to actual world `X=3.00-3.78m`, removes every local/object-transform remap, and leaves every other C7 field unchanged. Independent review returned `READY_FOR_DIAGNOSTIC_007_LOOP_CONTRACT`; C7-D4 records the accepted provenance.
3. Asset writer: **C7 closed DIAGNOSTIC_BLOCKED.** C7-T1 `a9acb0a`, C7-T2 `51fdbab`, C7-T2F `de625c6`, and C7-T2G `973d2fa` are committed and independently reviewed. The dry preflight returned `READY_FOR_BLENDER`, but the sole standard process failed before render on the dynamic Voronoi socket readback mismatch. The exact four-file evidence is frozen at `3937ef6`; no retry, evaluator, PNG review, export, or integration is permitted.
4. Independent diagnostic visual reviewer starts whenever the sole process leaves an exact, complete, closed 20-PNG set, whether the runner stopped before or after evaluator invocation. It may write `docs/workstreams/temple-tr4-visual-review/TR4_DIAGNOSTIC_REVIEW.md` and `THREAD_LOG.md` only, and must judge the four beauty frames plus masks/depth without repairing assets. An incomplete render set is frozen technical evidence but is not visually reviewed.
5. Runtime remains blocked after proof acceptance until `RUNTIME_ADOPTION.md` records exact accepted hashes and `ACCEPTED_FOR_RUNTIME_INTEGRATION`, except for the explicitly user-authorized R1 source scope below. Wildcard or “direct tests” authority is forbidden.
6. After the last source edit, exactly one final typecheck, one complete Vitest suite, one production build, and one browser-evidence pass. Independent QA is read-only after a candidate SHA.

`src/game/core/**`, `src/game/runtime/**`, deterministic commands/ticks, generator fairness, collision volumes, replay/hash, package/configuration, historical A3-A6 paths, and `docs/logs/CHANGELOG.md` remain frozen until final coordinator acceptance. Every workstream maintains its own `THREAD_LOG.md`.

The binding construction, camera, material, lifecycle, anti-clipping, semantic-root, export, and budget gates are in `docs/workstreams/temple-tr4-coordination/TR4_VISUAL_CONTRACT.md`.

### TEMPLE-TR4-R1 — runtime canyon replacement (authorized, in progress)

This user-authorized visible-runtime slice supersedes the earlier sequencing hold for its exact scope only. It may change exactly `src/game/render/tideScarWorld.ts`, `src/game/render/tideScarWorld.test.ts`, and `src/game/render/WorldRenderer.ts`. It replaces the inward panorama/card approach with deterministic near/mid/far canyon geometry, a physically thick fractured route surround, and depth-safe atmospheric separation. It must preserve simulation, collision, replay hashes, one-canvas ownership, public input, and all existing lifecycle cleanup. No external asset, Blender launch, export, package/configuration change, or material-socket-probe path is permitted.

The checkpoint requires a targeted renderer/world test, typecheck, and a manually inspected local browser gameplay frame before any follow-on hazard, character, or UI work. Its status remains in progress until that evidence exists; it does not accept the broader TR4 asset or runtime program.

R1A's bounded claim is only that the runtime replaces panorama/cards with deterministic closed near/mid/far terrain, a thick modular causeway, stable responsive camera records, and the opening/ending-only pursuer lifecycle, with real zero-error browser frames. R1B's bounded claim is only that portrait projection reduces road/runner scale to about `55-70%` of the R1A projection and that deterministic strata/talus fragments are embedded into their host terrain without a new draw call. Its three responsive tick-60 frames are clean, but they do not accept the overall visual program.

R1C's bounded claim is only that 12 deterministic, closed, independently reconstructed horizon components provide three actual depth bands, genuine cross-band and lateral gaps, and positive central portrait projection while remaining merged into the existing far mesh. Its final three responsive frames are clean and retain road/hazard/turn readability, but the same frames explicitly block overall quality because the masses still read as giant boxes and vertical walls.

R1D is closed at `ec00e44`. Its 12 existing horizon islands now use actual two-shelf terraced solids with independently varied heights, retreats, upper/lower notches, and bounded overhangs while keeping the same island count, depth placement, real gaps, camera, draw-call count, and world-coordinate pipeline. Tests reconstruct the actual position/index data and prove connected components, closed/outward/non-degenerate topology, distinct shelf/riser cycles, 12 unique profiles, XZ-normalized overhangs, retained gaps, and road-safe projection. The preserved three-profile tick-60 browser batch is clean; independent test and visual review both return `READY` for this narrow geometry claim and `MANUAL_BLOCKED` for overall fidelity.

Coordinator housekeeping H1 is closed at `0198c7a`. `.gitignore` now excludes generated Playwright report/result/blob/trace outputs, browser-profile directories, and additional numbered Blender backup files while leaving both protected material-socket-probe scripts, tracked evidence, ordinary source, and documentation visible.

R1E may continue in the same exact three-file subsystem scope as one normal source checkpoint of at most 500 added or modified lines. It is a surface-readability pass only: use the already tracked clean-room basalt texture and deterministic face-aware world UV/value treatment so top planes, terrace shelves, vertical walls, notch returns, and undersides read as one continuous rock material with decreasing contrast from near to mid to far. It may modify canyon material binding and geometry attributes but must preserve all R1D massing, island/mesh/draw-call counts, camera and lighting records, road/hazard/character/UI geometry, simulation/runtime rules, texture budgets, fallback behavior, and resource disposal. It may not add assets or objects, launch Blender, create formal evidence, or touch the protected probe paths.

R1E tests must inspect actual geometry/material state rather than metadata alone: all three canyon bands receive or clear the non-owning basalt map together; UVs are finite, world-scaled, deterministic, and non-collapsed on top and wall faces; face categories have deliberate readable value separation; near/mid/far variation decreases without flattening silhouettes; and map clearing plus disposal remain safe. Tests classify actual indexed triangles from reconstructed normals as upward at `normalY>.55`, underside at `normalY<-.55`, and vertical/return otherwise. Accepted dominant-plane UV scales are `.080-.100`, `.060-.080`, and `.045-.060 UV/m` for near/mid/far, strictly descending with a near/far ratio below `2.2`. Mean vertex-colour linear luminance must preserve `top > shelf > vertical/return > underside` in every actual horizon component; the normalized top-to-underside contrast is strictly near `>` mid `>` far with far still `>.12`, and within-category deterministic variation is also strictly near `>` mid `>` far. The real material path must keep emissive at zero, use map strength strictly near `>` mid `>` far with adjacent differences at least `.15`, and raise only the non-emissive albedo floor strictly near `<` mid `<` far with adjacent differences at least `.03` and every floor `<=.25`.

The R1D position/index arrays are immutable. Before R1E source acceptance, tests must bind independently captured `ec00e44` position-plus-index fingerprints for the abyss and all three named canyon meshes, require exactly four meshes with one material and zero geometry groups, and compare position/index/normal/UV/colour/material determinism across two independent worlds. Production `userData`, self-produced runtime hashes, `needsUpdate`, or boolean lifecycle flags cannot satisfy these gates. One targeted world/renderer suite, one development typecheck, `git diff --check`, and a fresh tick-60 desktop/portrait/landscape browser batch are required; the browser record must retain `31` draw calls and `27,896` triangles. Manual review must reject stretched seams, checker/noise shimmer, flat-color slabs, crushed-black walls, or any loss of road/hazard/turn readability. Passing R1E does not accept the causeway, characters, hazards, motion, broader TR4 asset program, or final game.

R1E remains open after candidate `cf5be8b4a761269c398dd03ed6152ff993f0f926`. The source candidate changes only `tideScarWorld.ts` and its direct test, remains within the 500-line checkpoint limit, preserves the four immutable position/index fingerprints, and passes its code gates. However, the fresh independent visual gate against `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-surface-v3-20260721T022515\` is `BLOCKED`: all three tick-60 profiles report one canvas, zero gameplay DOM/overflow/problems, `31` draw calls, and `27,896` triangles, but the albedo-floor-only change leaves repeated top-mouth/internal-return cavities visually unchanged and nearly black.

R1E-V4 candidate `2569c7700c6d5ffe9a1cf78b17d7b7225cc07dab` adds the previously authorized existing-HemisphereLight weight floor in only `src/game/render/tideScarWorld.ts` and `src/game/render/tideScarWorld.test.ts`. Targeted `7/7`, final `17 files / 71 tests`, typecheck, diff check, and the no-clean production build at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v4-build-20260721T110938\` pass. Candidate-bound static and motion evidence is preserved at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v4-candidate-2569c77-20260721T111255\`; every profile/tick keeps one canvas, zero gameplay DOM/overflow/problems, `31` draw calls, and `27,896` triangles. Independent visual review is nevertheless `BLOCKED`: landscape top-mouth dark pixels are `86.91%-93.98%` unchanged within one luma level, and desktop/portrait retain the same crushed internal returns. The `.135` floor is below the `.225` minimum base weight of the vertical/return category, so it cannot correct those faces.

R1E-V5 is authorized only in the same two source/test files. It may add one deterministic face attribute for a bounded additive shift of the scene's existing HemisphereLight sky/ground mixing weight immediately after Three r185 `<lights_fragment_begin>` and before native light maps/end and AO: top/shelf/wall shift `0`, return shift `.10`, underside shift `.15`, and effective weight `min(1, baseWeight + shift)` with a hard `shift <= .15`. The existing `.025/.030` albedo face lifts remain unchanged; position/index data, geometry, draw calls, camera, lights, exposure, and all other subsystems remain immutable. With no hemisphere light the correction is exactly zero. The V5 program cache key and actual geometry/shader tests must bind the new attribute, exact shifts, ordering, cap, deterministic replay, and forbidden-assignment list. It may not assign normals, direct/specular light, outgoing light, emissive, exposure, shadows, or post-AO colour. Fresh GPU, three-viewport, motion, and independent visual gates are required.

R1E-V5 candidate `a1aae169df2981aebe99c1edef0cf8efca98f406` passes targeted `7/7`, final `17 files / 71 tests`, typecheck, diff check, and the no-clean production build at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v5-build-20260721T114028\`. Its candidate-bound static and motion evidence at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v5-candidate-a1aae16-20260721T114115\` also keeps one canvas, zero gameplay DOM/overflow/problems, `31` draw calls, and `27,896` triangles. V5 produces a real dark-region lift, but independent visual review remains `BLOCKED`: the normal-size top-mouth row still reads as black wedges. Read-only dark-pixel ray attribution in `raycast-dark-attribution-v2.json` proves the main offenders are the existing near-horizon components with seeds `409` and `421`, whose return faces remain about luma `28-38`; two desktop outer ROIs are genuine no-hit gaps, and almost no failed sample sees an underside. Further light or underside tuning is therefore unauthorized.

R1E-V6 is a narrow horizon-notch geometry exception authorized only in `src/game/render/tideScarWorld.ts` and `src/game/render/tideScarWorld.test.ts`. It may change only the seeded upper/lower cut and shoulder factors of the existing horizon islands: near cut `.60 + unit*.12` and shoulder `.84 + unit*.08`; mid cut `.50 + unit*.10` and shoulder `.80 + unit*.08`; far must retain V5 cut `.38 + unit*.10` and shoulder `.74 + unit*.10`. It may not add or remove a vertex, index, triangle, mesh, group, object, asset, card, plane, or continuous wall; reposition island centres; change shelf runs/details/abyss; or change materials, shader, lights, camera, road, actors, hazards, UI, simulation, or lifecycle. The existing genuine gaps remain open. Tests must prove exactly `12` horizon components / `96` triangles each, unchanged vertex/index counts and exact far-index SHA-256 `15c61b15613bc21f5aba67e4b5ba4b6c22ebe80f80c40e640b5c480ebb12c402`, unchanged abyss/near/mid position hashes, deterministic replay, the exact band ranges from actual positions, and a new independently captured far position fingerprint. Browser output stays `31` calls / `27,896` triangles. Fresh three-viewport and desktop/landscape tick `60/68/76` evidence plus independent visual review must reject a repeated black-mouth row, flattened roof, erased negative space, or loss of basalt/route readability.

R1E-V6 candidate `6fe107f6307e30b6ff613c53613bf737f5f0ffa1` passes targeted `7/7`, final `17 files / 71 tests`, typecheck, diff check, and the no-clean production build at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v6-build-20260721T121230\`. Candidate-bound static and motion evidence is preserved at `C:\Users\Alex Chen\AppData\Local\Temp\tide-relay-r1e-v6-candidate-6fe107f-20260721T121400\`; every record keeps one canvas, zero gameplay DOM/overflow/problems, `31` calls, and `27,896` triangles. Independent visual review is still `BLOCKED`: desktop retains the large dark left-middle top opening, landscape retains a repeated dark-wedge row, and portrait retains a black roof silhouette. Same-coordinate comparison shows a real but insufficient lift, including desktop left-near `+7.72` luma with `74.6%` still below luma `45`, and landscape right `+5.69` with `88.7%` still below `45`. Tier-level ray attribution proves the remaining visible samples still hit near/mid `upper-riser` triangles, with actual normals as low as `normalY=-.916`; V6 changed both sides of that riser together and therefore did not correct its downward slope.

R1E-V7 is authorized only in the same two source/test files. It may change only the existing `tier.cuts === 1` intermediate-ring radius for near and mid horizon islands: after the existing per-step overhang multipliers have been applied, its final effective radial scale must equal the corresponding upper-ring final scale plus exactly `.04`. The existing overhang step remains `1.14` on the upper ring and `.92` on the intermediate ring, so the intermediate pre-ledge expression may compensate those frozen multipliers; every V6 cut/shoulder factor, ring angle, base radius, centre/offset, height/drop, other tier, and the complete far-band transition expression remain byte-equivalent in behavior. This is a slope correction, not a new shelf, roof, wall, light, or material. It must add or remove no vertex, index, triangle, mesh, group, object, asset, card, or plane and must preserve all genuine gaps, draw calls, triangles, camera, road, actors, hazards, UI, simulation, and lifecycle. Direct tests must reconstruct the actual 128 near/mid upper-riser triangles and require every one to remain a return/wall plane with `.045 <= normalY < .55`, zero downward-facing members, and an actual final/post-ledge per-step intermediate-minus-upper radial scale of `.04`. That bridge replaces the old positive upper-overhang assertion only for near/mid: reconstructed upper-minus-intermediate radial deltas must all remain outward with component minima `>-1m` and maxima `<-.2m`, while every far component retains the prior positive `maxOverhang >.35m` gate. Because the outward bridge narrows only the adjacent near/mid lower shelf, its actual outer/inner area ratio must remain `>1.23`; every upper shelf and every far-band shelf retain the prior `>1.25` gate. Far-band horizon positions and the exact far index SHA remain frozen, abyss/near/mid raw position hashes remain unchanged, and deterministic replay plus a new far-mesh position fingerprint are mandatory. Fresh three-viewport tick-60 and desktop/landscape tick `60/68/76` evidence plus independent visual review must reject any remaining repeated black-mouth row, flattened/continuous roof, gray or glowing ramp, erased negative space, shimmer, or lost basalt/route readability.

R1F is a deferred design only and is **not source-authorized** until R1E-V7 is independently accepted. When separately reauthorized, it may change exactly `src/game/render/WorldRenderer.ts` and `src/game/render/tideScarWorld.test.ts` as one normal source checkpoint of at most 500 added or modified lines. It is a causeway presentation pass only: retain the six deterministic instanced module signatures, `6-11m` module lengths, continuous start/end cross-sections, canonical route transforms, collision and gap semantics, protected playable corridor, camera, canyon, actors, hazards, UI, asset set, lifecycle, and simulation/replay state. It must turn the current broad yellow strip into one continuous warm sandstone structure through actual deterministic medium-scale top-panel and broken-lip geometry plus a face-aware, world-anchored PBR response. It may not add a render object or asset, launch Blender, change section generation, invent a false safe lane or gap, or touch the protected probe paths.

R1F direct tests must reconstruct all six actual geometries rather than trust `userData`: prove deterministic position/index/normal/UV/colour state, outward non-degenerate side/cap topology, connected top coverage across the protected corridor, continuous terminal cross-sections, at least three distinct non-coplanar top-panel signatures, and real asymmetric lip/return depth without a full-width open seam. Surface values must read `top > worn edge > return/underside`, remain zero-emissive, and keep the tracked sandstone map non-owning and safely clearable/disposable. The final fresh browser batch must retain exactly `31` draw calls, remain at or below `32,000` triangles, and cover tick-60 desktop/portrait/landscape plus desktop/landscape ticks `60/68/76`. Manual review must reject a featureless yellow band, repeated tile stamp, full-width dark seam, false hole, floating lip, texture swim/shimmer/moire, or any loss of runner/hazard/turn readability. Passing R1F will not accept the courier, pursuer, hazards, animation, broader asset program, or final game.

### Recovery-only atomic checkpoint exceptions

These exceptions authorize preservation commits only and do not authorize new code:

- C02 owns exactly `tools/temple-asset-pipeline/generate_tide_scar_tr4_pack.py`, `evaluate_tide_scar_tr4_pack.py`, and `run_tide_scar_tr4_pack.py`. They exceed 500 lines because C6 preflight/evidence binds the closed three-script hash set; splitting it would falsify provenance. Verification is AST parse plus exact C6 launch/status/hash replay without running the consumed diagnostic.
- C07 and C08 each own the exact stage-specific A0/A2 generator, evaluator, and runner named in `BACKLOG_SPLIT_PLAN.md`; C10 and C14 own their exact A3/A5 generator/evaluator pairs. Their generated metadata binds each family, so a family is one historical preservation claim. Verification is AST parsing plus the matching immutable metadata/hash record.
- C26 owns exactly `src/game/render/WorldRenderer.ts` and `WorldRenderer.test.ts`. The inherited renderer delta exceeds 500 lines and has no typecheckable partial-file checkpoint. Verification is typecheck plus only the direct renderer test.

Owners are the coordinator for contract/evidence checkpoints, one bounded asset-pipeline writer for C02/C07/C08/C10/C12/C14, and one bounded renderer writer for C19-C28. No writer may begin the next checkpoint before the current cached path list, checks, and local commit are reported.

### C7 atomic source-checkpoint exception — active after C7-D4

C7-T1 owns exactly `tools/temple-asset-pipeline/generate_tide_scar_tr4_pack.py`; owner is the single Temple TR4 asset writer. The reconstruction spans the file's mutually dependent scene construction, material binding, compositor, depth pass, semantic roots, metrics, and 20-write loop. The runner hashes this exact generator before it can author a fail-closed preflight, so partially staged intermediate versions would either falsify provenance or create a source checkpoint that cannot satisfy the closed construction contract. C7-T1 is therefore one path and may exceed 500 changed lines only after independent contract readiness and a separate coordinator authorization. Whole-checkpoint verification is exact-path cached-set inspection, `git diff --cached --check`, UTF-8, Python AST parse, static forbidden-path/import/output audit, and canonical construction-schema/hash replay without importing `bpy`, creating `diagnostic-007`, or launching Blender. This exception does not extend to runner/evaluator, evidence, export, or runtime files.

### Material-socket-probe-001 atomic runner checkpoint exception — review requested

The proposed exception owns exactly `tools/temple-asset-pipeline/run_tide_scar_material_socket_probe.py`; owner is the single Temple TR4 asset writer. The outer runner must keep repository/branch/independent-Git verification, pure-Python committed-blob comparison, four exact authorization-line gates, canonical input and two-schema replay, seven hashes, a static audit of both scripts, exactly one non-loop Blender subprocess call, independent 11/19/25 evidence replay, and all four terminal directory closures in one HEAD-bound launch boundary. Splitting those mutually dependent gates across commits would leave an executable checkpoint whose own hash/provenance or fail-before-root claim cannot be reviewed as a closed unit, while adding a third helper would exceed the exact two-script contract. The generator remains a separate normal source checkpoint under 500 lines.

The runner may exceed 500 changed lines only after a fresh independent verdict `READY_FOR_MATERIAL_SOCKET_PROBE_001_RUNNER_ATOMIC_EXCEPTION` and a separate coordinator reauthorization. Whole-checkpoint verification is exact-path cached-set inspection, `git diff --cached --check`, UTF-8 AST parsing of both scripts, canonical JSON parsing, exact 11/19/25/4 replay, exact source-token/source-record counts of one, exact future process-token/process-record counts of zero, one `subprocess.run` call site outside every loop, no other process path, static absence of render/evaluator/export/retry/artifact extensions, pure-Python loose-Git HEAD binding review, and confirmation that `probe-001` remains absent. It must not execute either script, import `bpy` outside Blender, launch Blender, create output, stage evidence, or weaken the separate final-hash launch checkpoint. The current oversized runner is frozen as WIP_UNCOMMITTED during this review and has not been executed, staged, or committed.

## Historical TR3-005C/A3-A6 state — preserved, not current authority

### TR3-005C — Tide Scar root-cause visual/render correction

Historical branch: `codex/temple-run` (migrated to standalone `main`)
Required baseline: `52ae9ae631fa3761f8f8737978af1840ba2df8a4`
Status: **in progress**. D4A design chain `c75621be9f2216791571b2daaa799587bb65050b -> 76097083d062c3454b5478037ec074fd12e96436` is read-only direction; 005C preserves the existing dirty TR3 work, corrects renderer-only composition/material/evidence roots, and does not grant an intermediate candidate, full matrix, full suite, build, commit, or push.

TR2 remains the accepted rules/runtime baseline. TR3 translates the separately approved Tide Scar design into an original, stylized Three.js presentation within the frozen render/UI/evidence paths below.

### Required outcome

Refine the existing complete third-person runner without changing its clean-room boundary:

- deterministic procedural course generation and replayable simulation state
- continuous acceleration and three-lane movement
- jump, slide, correct/incorrect 90-degree turns, gaps, blockers, pickups, and shield
- fair generation with reaction-distance validation
- distance, score, shards, multiplier, best score, pause, restart, and game over
- fixed-step runtime and smooth interpolated Three.js presentation
- runner animation, chase pressure, camera follow, turn camera, impacts, and procedural audio
- keyboard, real one-finger swipe, and visible accessible controls
- responsive desktop, portrait, and landscape layouts
- reduced motion, high contrast, audio toggle, and lifecycle cleanup
- unit tests, deterministic replay tests, build, browser interaction, screenshots, and performance evidence
- canonical visible pursuit pressure, 250 m milestone events, score-first HUD hierarchy, articulated gait, mineral causeway palette, and distinct obstacle silhouettes
- recoverable non-gap stumbles that deterministically close pursuit distance, with repeated misses leading to a reachable pursuer capture
- milestone, close-chase, beam, ring, column, and gap QA states reproduced exclusively from public command traces
- browser assertions for exact HUD values, pursuer clipped bounds/area, per-kind obstacle clipped bounds, eight-phase continuous limb motion, and non-capture `pursuerGapPx >= 6`

### Frozen TR3-005C production scope

May change only:

- `src/game/render/theme.ts`, `src/game/render/WorldRenderer.ts`, `src/game/render/runnerRig.ts`, `src/game/render/pursuerRig.ts`, `src/game/render/tideScarWorld.ts`, and their direct tests; new render-only `d4Profile.ts`, `d4Assets.ts`, and `tideScarRoad.ts`
- `src/App.tsx`, `src/styles.css`, and `scripts/capture-temple-evidence.mjs`
- `docs/DESIGN.md`, this file, `docs/qa/TEMPLE_ACCEPTANCE.md`, `docs/workstreams/temple-tr3/THREAD_LOG.md`, development-only `docs/screenshots/temple/tr3-d4-dev/**`, and `src/assets/tide-scar-d4/` containing only the D4A manifest and seven approved runtime derivatives

Must not change `src/game/core/**`, `src/game/runtime/**`, rules, generator, canonical state, collision boxes, lanes, speed curve, stumble/capture behavior, replay/hash, package/config files, or `docs/logs/CHANGELOG.md`. Do not cherry-pick D4 design commits. Visual seed data derives only from section/event identity and cannot enter canonical state or hashes. The D4 composite/north-star/source PNGs are forbidden from runtime; no gameplay additions, false metrics, static mockup evidence, DOM gameplay cells, or imported/copy-pasted external assets are allowed.

### TR3-005C development gate

- Retain one WebGL canvas, bounded eight-section course, fixed-step interpolation, and canonical event-driven visuals. Profile order is landscape, portrait, desktop with frozen FOV `46/40/43`; speed does not ramp FOV.
- D4A tier selection occurs before texture upload; capture verifies the exact four selected desktop or mobile files, not merely request count. Desktop `22.667 MiB` stays within `38 MiB`; mobile/low-GPU `6.667 MiB` stays within `18 MiB`. Every load failure has a non-blocking flat-PBR fallback and destroy releases all resources.
- The development-only real-runtime batch is desktop normal/column, portrait normal/close, and landscape running. It proves one canvas, zero gameplay DOM, zero overflow/errors, fixed profile/FOV plus camera pitch/horizon/vanishing-point/road/runner bands, current asset request set, texture estimate, real runner/pursuer/hazard/scar geometry, HUD-content separation, and canonical-state equivalence.
- Tide Scar vertices remain in the road right third, use 2–5 deterministic pre-clip intervals with 42–64% requested coverage, remove only local canonical-gap spans, and have zero presentation overlap with unresolved beam/ring/column/gap silhouettes. Evidence records clipped segment polygons rather than a global ribbon AABB.
- Draw calls include shadows and remain at most 46 desktop / 38 mobile; triangles remain below 190k desktop / 115k mobile and preparation p95 is below 12 ms. This is a development gate only; formal evidence remains unauthorized.

### Non-goals

- copied Temple Run branding, characters, monsters, art, music, UI, screenshots, or source
- shops, ads, accounts, purchases, online leaderboards, unlock trees, or multiple environments
- importing implementation from the standalone `reproduction-tetris` repository
- shipping another game mode, rule change, or fake gameplay entity

### Completion gate

Stop after the named targeted suites, one typecheck, and one development visual batch. Do not run the full suite/build/formal evidence, commit, push, or contact independent QA. A later coordinator authorization controls candidate gates.

### TEMPLE-TR3-A3 IN PROGRESS — not integrated

A3 is an additive, offline structural clay proof only. It may create a new deterministic Blender generator and A3-only proof outputs without reading or modifying `src/**`, canonical state, runtime/rules, browser evidence, package/config, root changelog, or any existing A0/A1/A2/TR3 asset/proof path. It renders exactly one low-resolution portrait/desktop/closeup clay batch and stops on either a fail-closed structural verdict or a ready clay verdict. Final materials, GLB/KTX2 export, optimization, runtime/browser work, candidate creation, commit, push, and QA remain unauthorized.

### TEMPLE-TR3-A4 IN PROGRESS — not integrated

A4 is one offline clay correction batch only. Its new A4 generator may derive the A3 scene but may change only camera framing, pursuer transform/rig silhouette, and ring/support geometry; all causeway, canyon, clay material, Tide Scar, route-count, and triangle-budget data remain A3-equivalent. Run one Blender 4.5.5 portrait/desktop/closeup batch and one deterministic/manual gate, then stop with READY or BLOCKED. No final materials, export, runtime, browser, commit, push, or QA activity is authorized.

### TEMPLE-TR3-A5 - authorized offline composition proof, not integrated

Status: **in progress, one clay batch only.** A3 and A4 are closed **BLOCKED** and remain byte-for-byte evidence baselines. The sole A5 writer may append only this contract/log set and add only tools/temple-asset-pipeline/generate_tide_scar_hero_a5.py, tools/temple-asset-pipeline/evaluate_asset_proof_a5.py, and docs/workstreams/temple-tr3/asset-proof/a5/**. No src, runtime, UI, core, rules, package, config, root changelog, assets outside A5, browser evidence, Git, commit, push, or QA path is authorized.

One Blender 4.5.5 process renders the same chase state to portrait 540x960, desktop 960x540, landscape 844x390 beauty/object-ID/road/depth, plus closeup 640x480 beauty. One evaluator then runs once and four beauty frames receive one manual inspection. Exact source comparisons retain A4 baselineFailures and preserve seed 270803, fixed roots, clay values, 16/17/125.949m/120.494deg/5.95m/.68 route, 6/10/4 canyon with three portrait openings and no wall, editable clipped right-edge Tide Scar, and <=45,000 triangles.

Fail-closed gates: 16 decorated caps, >=32 attached side fragments, signature run <=2, 20 unique canyon signatures/no continuous wall; route-frame quadruped; two-contact supported arch with physical aperture; real curve/spline Scar mask with two right-third runs/no semantic intersections; portrait courier x=.45-.55,y=.60-.68 and pursuer x=.43-.57,y=.74-.83, overlap >=.045W, gap .020-.075H, .02 containment, >=32px; closeup pursuer >=48px; profile hazard height/opening >.15H/.10W, >.20H/.09W, >.22H/.11W. All hero profiles require p50>.12, p95>.55, near-black <10 percent, route ROI p50>.25, positive semantic ROIs, no fog/panorama artifact, non-contiguous near/mid/far masks, and two occlusion boundaries. Maximum success is READY_FOR_VISUAL_REVIEW, never final acceptance; a blockout, featureless road, or low-poly strip wall is BLOCKED.

### TEMPLE-TR3-A6 — bounded no-render recovery preflight

Status: **in progress; documentation only.** A5 is closed BLOCKED and must remain immutable. A6 may append only the named contracts/logs and add only `docs/workstreams/temple-a6-preflight/**`; it must not create a generator, evaluator, proof image, metadata artifact, asset, or runtime change. Blender, rendering, browser/capture, image generation, network, export, product code, package/configuration, build/test, Git, candidate, commit, push, and QA are forbidden.

A6 freezes a later preflight input/result schema before any future Blender authorization. It resolves clay material data only from A4, never from absent A3 keys, and rejects dynamic metadata access after preflight. It also freezes the original central chase-line, profile bands, physical road/canyon/arch/Tide Scar construction constraints in `docs/workstreams/temple-a6-preflight/`. Every preflight check must be green before a fresh coordinator authorization can start a new Blender batch; no existing threshold may be lowered.
